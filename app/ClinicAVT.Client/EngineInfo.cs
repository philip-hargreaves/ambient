namespace ClinicAVT.Client;

public static class EngineInfo
{
    public const string Name = "clinicavt";
    public const string Version = "0.1.0";

    // The engine's default, mirrored on the native side in engine_main.cpp
    public const string PipeName = "LOCAL\\clinicavt-engine";
}

