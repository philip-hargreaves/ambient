namespace ClinicAVT.App.Core.Preferences;

/// <summary>Value is the engine's name for the option and Name is what the combo shows.</summary>
public sealed record NoteOption(string Value, string Name);
