<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="Ambient.App.Shell.StatusBarView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Border
        Padding="24,6"
        Background="{ThemeResource LayerFillColorDefaultBrush}"
        BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
        BorderThickness="0,1,0,0">
        <Grid ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!-- One status, replaced as things happen. The ring means "in progress" -->
            <StackPanel Orientation="Horizontal" Spacing="8">
                <ProgressRing
                    Width="14"
                    Height="14"
                    IsActive="{x:Bind ViewModel.Busy, Mode=OneWay}"
                    Visibility="{x:Bind ViewModel.Busy, Mode=OneWay}" />
                <TextBlock
                    VerticalAlignment="Center"
                    Style="{StaticResource CaptionText}"
                    Text="{x:Bind ViewModel.DisplayLabel, Mode=OneWay}" />
            </StackPanel>

            <Border Grid.Column="1" />

            <!-- The two models at work, each with its live figure inside its
                 own chip. Tabular figures so digits change without jitter -->
            <StackPanel
                Grid.Column="2"
                Orientation="Horizontal"
                Spacing="8"
                VerticalAlignment="Center">
                <Border
                    Background="{ThemeResource SubtleFillColorSecondaryBrush}"
                    Style="{StaticResource ChipBorder}"
                    Visibility="{x:Bind ViewModel.AsrChipVisible, Mode=OneWay}">
                    <!-- Colour pairs swapped by visibility: code-resolved
                         brushes follow only the OS theme -->
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <Ellipse
                            Width="6"
                            Height="6"
                            VerticalAlignment="Center"
                            Fill="{ThemeResource SystemFillColorSuccessBrush}"
                            Visibility="{x:Bind ViewModel.AsrActive, Mode=OneWay}" />
                        <Ellipse
                            Width="6"
                            Height="6"
                            VerticalAlignment="Center"
                            Fill="{ThemeResource ControlStrongFillColorDisabledBrush}"
                            Visibility="{x:Bind ViewModel.AsrResting, Mode=OneWay}" />
                        <TextBlock
                            Style="{StaticResource CaptionText}"
                            Text="{x:Bind ViewModel.AsrChip, Mode=OneWay}"
                            Typography.NumeralAlignment="Tabular"
                            Visibility="{x:Bind ViewModel.RealtimeHealthy, Mode=OneWay}" />
                        <TextBlock
                            Foreground="{ThemeResource CautionBrush}"
                            Style="{StaticResource CaptionText}"
                            Text="{x:Bind ViewModel.AsrChip, Mode=OneWay}"
                            Typography.NumeralAlignment="Tabular"
                            Visibility="{x:Bind ViewModel.RealtimeLow, Mode=OneWay}" />
                    </StackPanel>
                </Border>
                <Border
                    Background="{ThemeResource SubtleFillColorSecondaryBrush}"
                    Style="{StaticResource ChipBorder}"
                    Visibility="{x:Bind ViewModel.NoteChipVisible, Mode=OneWay}">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <Ellipse
                            Width="6"
                            Height="6"
                            VerticalAlignment="Center"
                            Fill="{ThemeResource SystemFillColorSuccessBrush}"
                            Visibility="{x:Bind ViewModel.NoteActive, Mode=OneWay}" />
                        <Ellipse
                            Width="6"
                            Height="6"
                            VerticalAlignment="Center"
                            Fill="{ThemeResource ControlStrongFillColorDisabledBrush}"
                            Visibility="{x:Bind ViewModel.NoteResting, Mode=OneWay}" />
                        <TextBlock
                            Style="{StaticResource CaptionText}"
                            Text="{x:Bind ViewModel.NoteChip, Mode=OneWay}"
                            Typography.NumeralAlignment="Tabular" />
                    </StackPanel>
                </Border>
                <!-- The app's own footprint: not a worker, so no dot -->
                <Border
                    Background="{ThemeResource SubtleFillColorSecondaryBrush}"
                    Style="{StaticResource ChipBorder}"
                    Visibility="{x:Bind ViewModel.MemoryChipVisible, Mode=OneWay}">
                    <TextBlock
                        Style="{StaticResource CaptionText}"
                        Text="{x:Bind ViewModel.MemoryChip, Mode=OneWay}"
                        Typography.NumeralAlignment="Tabular" />
                </Border>
            </StackPanel>

            <!-- Partner marks from Assets/logos/credits.json. Change them per
                 showcase in that file -->
            <StackPanel
                x:Name="CreditsRow"
                Grid.Column="3"
                Margin="8,0,0,0"
                Orientation="Horizontal"
                Spacing="12" />
        </Grid>
    </Border>
</UserControl>
